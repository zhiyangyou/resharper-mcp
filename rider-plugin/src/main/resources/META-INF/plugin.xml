<idea-plugin require-restart="true">
  <id>com.j-light.resharper-mcp</id>
  <name>MCP Server for Code Intelligence</name>
  <version>0.10.0</version>
  <vendor email="j.light.developer@gmail.com" url="https://plugins.jetbrains.com/vendor/j-light">Joshua Light</vendor>
  <idea-version since-build="253" />
  <depends>com.intellij.modules.rider</depends>
  <description><![CDATA[
    <p>An MCP (Model Context Protocol) server that runs inside the ReSharper/Rider backend process,
    exposing code intelligence features to AI assistants via HTTP.</p>

    <p>Available tools:</p>
    <ul>
      <li><b>find_usages</b> — Find all references to a symbol</li>
      <li><b>get_symbol_info</b> — Get detailed symbol info: kind, type, params, docs, base types</li>
      <li><b>find_implementations</b> — Find implementations of interfaces/abstract classes</li>
      <li><b>get_file_errors</b> — Get compile errors and unresolved references</li>
      <li><b>search_symbol</b> — Search symbols by name across the solution</li>
      <li><b>go_to_definition</b> — Navigate to a symbol's declaration with source text</li>
      <li><b>get_solution_structure</b> — List projects, target frameworks, and references</li>
      <li><b>browse_namespace</b> — Browse namespace hierarchy</li>
      <li><b>list_symbols_in_file</b> — List all declarations in a file</li>
      <li><b>list_solutions</b> — List all open solutions across Rider instances</li>
      <li><b>fix_usings</b> — Fix missing using directives in C# files</li>
      <li><b>format_file</b> — Format, clean up, or apply code style to a file</li>
      <li><b>flow</b> — Describe control flow of a method or type with execution steps, branches, and inlined calls</li>
      <li><b>get_symbol_source</b> — Get the full declaration source code of a symbol</li>
      <li><b>get_call_hierarchy</b> — Build an incoming/outgoing call hierarchy tree for a method</li>
      <li><b>get_type_hierarchy</b> — Get the inheritance hierarchy of a type (super/subtypes)</li>
      <li><b>get_diagnostics</b> — Run daemon inspections on a file and report diagnostics with quick-fix availability</li>
      <li><b>list_quick_fixes</b> — List available ReSharper quick-fixes at a position</li>
      <li><b>apply_quick_fix</b> — Apply a ReSharper quick-fix at a position (write)</li>
      <li><b>rename_symbol</b> — Semantic rename of a symbol and all references across the solution (write)</li>
      <li><b>generate_members</b> — Generate members on a type, e.g. constructors and overrides (write)</li>
      <li><b>complete_at</b> — Get code completion suggestions at a position</li>
      <li><b>apply_suggestions</b> — Apply inspection quick-fixes file-wide by id, e.g. convert to primary constructor (write)</li>
    </ul>

    <p>Supports C#, F#, VB, and any language with a ReSharper PSI implementation.</p>

    <p><b>Multi-solution support:</b> multiple Rider instances can run simultaneously — all accessible
    through a single MCP endpoint. Use the <b>list_solutions</b> tool to discover open solutions and
    target tools with the <code>solutionName</code> parameter.</p>

    <p>The MCP server starts automatically on <code>http://127.0.0.1:23741/</code> when you open a solution.
    Set the <code>RESHARPER_MCP_PORT</code> environment variable to use a different port.</p>
  ]]></description>
  <resource-bundle/>
  <getting-started><![CDATA[
    <h3>1. Configure your MCP client</h3>
    <p>Add to your MCP client config (e.g. Claude Code <code>settings.json</code>, Cursor, Windsurf, etc.):</p>
    <pre><code>{
  "mcpServers": {
    "resharper": {
      "type": "http",
      "url": "http://127.0.0.1:23741/"
    }
  }
}</code></pre>

    <h3>2. Open a solution</h3>
    <p>The MCP server starts automatically when you open a solution in Rider. No extra setup needed.</p>

    <h3>3. Use the tools</h3>
    <p>Your AI assistant can now call tools like <code>find_usages</code>, <code>get_symbol_info</code>,
    <code>search_symbol</code>, and more — powered by ReSharper's code analysis engine.</p>

    <h3>Multiple solutions</h3>
    <p>When multiple Rider instances are open, all solutions are accessible through the same
    <code>http://127.0.0.1:23741/</code> endpoint. Use <code>list_solutions</code> to see what's available,
    and pass <code>solutionName</code> to target a specific solution.</p>

    <h3>Custom port</h3>
    <p>Set the <code>RESHARPER_MCP_PORT</code> environment variable before launching Rider to use a different port.</p>
  ]]></getting-started>
  <change-notes><![CDATA[
    <p><b>0.10.0</b></p>
    <ul>
      <li>New <code>apply_suggestions</code> tool: apply inspection quick-fixes across a whole file by inspection id — e.g. convert every explicit constructor into a primary constructor. Position-free and file-wide (complements <code>apply_quick_fix</code>), driving ReSharper's "Fix all in file" engine headlessly. Filter with <code>inspectionIds</code> or pass <code>all</code>; preview with <code>dryRun</code>; batch via <code>filePaths</code></li>
    </ul>
    <p><b>0.9.0</b></p>
    <ul>
      <li>Nine new agentic tools for navigation, diagnostics, and refactoring</li>
      <li>Read/navigation: <code>get_symbol_source</code> (full declaration source), <code>get_call_hierarchy</code> (incoming/outgoing callers), <code>get_type_hierarchy</code> (super/subtype tree)</li>
      <li>Diagnostics: <code>get_diagnostics</code> (daemon inspections with severity and quick-fix availability), <code>list_quick_fixes</code> (available bulb actions at a position), <code>complete_at</code> (code completion suggestions)</li>
      <li>Write/refactoring: <code>rename_symbol</code> (semantic solution-wide rename with dry-run support), <code>generate_members</code> (generate constructors/overrides/etc.), <code>apply_quick_fix</code> (apply a bulb action at a position)</li>
    </ul>
    <p><b>0.8.0</b></p>
    <ul>
      <li>New <code>flow</code> tool: describe the control flow of a method or type — execution steps, branch conditions, loops, try/catch paths, inlined call targets (configurable depth), and why-hints from code comments</li>
    </ul>
    <p><b>0.7.0</b></p>
    <ul>
      <li>Batch mode: most tools now accept multiple inputs in a single call, reducing round-trips when querying several symbols or files at once</li>
      <li>Symbol-based tools (<code>find_usages</code>, <code>get_symbol_info</code>, <code>find_implementations</code>, <code>go_to_definition</code>) accept a <code>symbols</code> array</li>
      <li>File-based tools (<code>get_file_errors</code>, <code>list_symbols_in_file</code>, <code>fix_usings</code>, <code>format_file</code>) accept a <code>filePaths</code> array</li>
      <li><code>search_symbol</code> accepts a <code>queries</code> array; <code>browse_namespace</code> accepts a <code>namespaceNames</code> array</li>
      <li><code>find_usages</code> now supports <code>maxResults</code> parameter to cap results on widely-used symbols</li>
    </ul>
    <p><b>0.6.1</b></p>
    <ul>
      <li>Fix deprecated <code>URL(String)</code> constructor usage — replaced with <code>URI(String).toURL()</code> for Java 20+ compatibility</li>
    </ul>
    <p><b>0.6.0</b></p>
    <ul>
      <li>New <b>format_file</b> tool: format, clean up, or apply code style to files using ReSharper's CodeCleanupRunner</li>
      <li>Peer-to-primary promotion: when the primary Rider instance closes, a peer automatically takes over the primary port</li>
      <li>Watchdog health checks: periodic self-checks detect unresponsive servers and trigger recovery</li>
      <li>find_usages now transitively searches interface/base method usages — finds call sites through abstractions</li>
      <li>find_implementations supports <code>maxResults</code> parameter to prevent overflow on widely-implemented interfaces</li>
      <li>Smarter file resolution: relative paths, case-insensitive matching, and suffix/partial path matching</li>
      <li>Better error messages: unresolvable symbols show the reference name; wrong-kind symbols suggest the correct kind</li>
      <li>HttpListener robustness: consecutive errors trigger in-place listener recovery; restart waits for old thread to exit</li>
      <li>Source-generated and sourceless types handled gracefully instead of crashing</li>
      <li>Symbol resolution reordered: references resolved before declarations for correct cursor-on-usage behavior</li>
    </ul>
    <p><b>0.5.1</b></p>
    <ul>
      <li>Streamable HTTP transport: upgrade MCP protocol from 2024-11-05 to 2025-03-26, enabling compatibility with clients like Codex that use the newer transport</li>
      <li>GET requests now return a valid SSE stream (text/event-stream) instead of 405, so clients that probe with GET during discovery work correctly</li>
      <li>DELETE requests accepted for session termination per Streamable HTTP spec</li>
      <li>Mcp-Session-Id header sent on all responses; mismatched session IDs rejected with 404 per spec</li>
      <li>JSON-RPC notifications (no id) now correctly return 202 Accepted with no body</li>
      <li>Protocol version negotiation: server accepts both 2024-11-05 and 2025-03-26 clients</li>
      <li>Proper Allow header (GET, POST, DELETE, OPTIONS) on 405 responses</li>
    </ul>
    <p><b>0.5.0</b></p>
    <ul>
      <li>New <b>fix_usings</b> tool: automatically adds missing using directives in C# files by resolving unresolved type references against the solution and referenced assemblies</li>
      <li>Unambiguous types (single candidate namespace) are auto-fixed; ambiguous types are reported with candidates</li>
      <li>Interactive disambiguation: call again with <code>resolutions</code> parameter to resolve ambiguous imports (e.g. <code>{"ILogger": "Microsoft.Extensions.Logging"}</code>)</li>
      <li>First write-capable tool — introduces IMcpWriteTool marker interface and write lock dispatch infrastructure</li>
    </ul>
    <p><b>0.4.1</b></p>
    <ul>
      <li>Fix symbol-by-name resolution for partially qualified names (e.g. "MyClass.MyMethod") — previously failed when the input wasn't a full namespace path</li>
    </ul>
    <p><b>0.4.0</b></p>
    <ul>
      <li>Compact text output: all tools now return concise plain-text instead of verbose JSON, significantly reducing token usage for LLM consumers</li>
      <li>New FormatSignature helper produces one-line signatures like "static method DoStuff(x:int) : void"</li>
      <li>Members in get_symbol_info shown as compact signature lines instead of nested JSON objects</li>
      <li>search_symbol, find_usages, find_implementations, list_symbols_in_file, browse_namespace, get_solution_structure, get_file_errors all use compact text format</li>
    </ul>
    <p><b>0.3.1</b></p>
    <ul>
      <li>Path-segment matching for solutionName: use a unique directory name (e.g. "my-repo") instead of the full path to target a solution</li>
      <li>list_solutions now includes uniquePathSegment hints for solutions with duplicate names</li>
      <li>Increase default snippet length from 200 to 2000 characters for richer context in tool results</li>
    </ul>
    <p><b>0.3.0</b></p>
    <ul>
      <li>Multi-solution support: multiple Rider instances accessible through a single MCP endpoint using primary/peer architecture</li>
      <li>New list_solutions tool to discover all open solutions</li>
      <li>Optional solutionName parameter on all tools for targeting a specific solution</li>
      <li>Automatic stale peer cleanup when a Rider instance closes or crashes</li>
    </ul>
    <p><b>0.2.1</b></p>
    <ul>
      <li>Performance: search_symbol and symbol-by-name resolution now use R#'s symbol cache instead of brute-force PSI tree walking — orders of magnitude faster on large solutions</li>
      <li>Fix cascading timeout bug: timed-out requests no longer block the R# thread, preventing snowball failures when parallel tool calls queue up</li>
      <li>Increase tool execution timeout from 30s to 120s to handle queued parallel requests</li>
      <li>Fix macOS plugin install path (plugins/ subdirectory)</li>
    </ul>
    <p><b>0.2.0</b></p>
    <ul>
      <li>find_usages: results grouped by project and file with counts; excludeDeclarationFile option; per-line deduplication</li>
      <li>find_implementations: direct vs indirect classification with via chain</li>
      <li>get_symbol_info: filter compiler-generated record members (operators, accessors, parameterless ctors)</li>
      <li>search_symbol: dot-qualified queries (e.g. "IProfile.Fake") match by ContainingType.MemberName</li>
      <li>list_symbols_in_file: filter get_/set_/add_/remove_ accessor methods</li>
      <li>browse_namespace: flag generated source files</li>
    </ul>
    <p><b>0.1.0</b></p>
    <ul>
      <li>Initial release with 9 MCP tools</li>
      <li>Language-agnostic PSI support (C#, F#, VB)</li>
      <li>Symbol resolution by name or file position</li>
      <li>Ambiguity detection with candidate list</li>
    </ul>
  ]]></change-notes>
  <extensions defaultExtensionNs="com.intellij">
    <statusBarWidgetFactory
        id="ReSharperMcp.StatusBar"
        implementation="com.jlight.resharpermcp.McpStatusBarWidgetFactory"/>
    <toolWindow
        id="ReSharperMcp.Monitor"
        anchor="right"
        secondary="false"
        icon="AllIcons.General.Console"
        factoryClass="com.jlight.resharpermcp.McpToolWindowFactory"/>
  </extensions>
</idea-plugin>
